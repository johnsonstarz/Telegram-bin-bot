# Telegram Bot framework (v21.x — async-first)
python-telegram-bot==20.7

# Async HTTP client (for binlist.net lookups)
aiohttp==3.10.5

# Environment variable loading from .env files
python-dotenv==1.0.1
